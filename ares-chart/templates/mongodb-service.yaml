apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
spec:
  selector:
    app: mongodb
  clusterIP: None
  ports:
    - port: {{.Values.mongodb.service.port}}
      targetPort: {{.Values.mongodb.service.port}}
  
